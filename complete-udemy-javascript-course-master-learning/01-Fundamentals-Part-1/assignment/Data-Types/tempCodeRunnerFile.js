let country = "india";
let continent = "asia";
let population = "2.5";
let language = "Hindi";

const description = `${country} is in ${continent}, and its ${population} million people speak ${language}`;
console.log(description);

// In JavaScript, for example,( `) backticks are used to define template literals, which allow for embedded expressions. 